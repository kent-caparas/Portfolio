---
import { render } from 'astro:content';
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getProjects, projectTransitionName, statusLabel } from '@/features/projects/lib/projects';

export async function getStaticPaths() {
  const projects = await getProjects();
  return projects.map((project) => ({
    params: { slug: project.id },
    props: { project },
  }));
}

const { project } = Astro.props;
const { name, status, description, tags, repo, demo } = project.data;
const { Content } = await render(project);
---

<BaseLayout title={`${name}, Kent Caparas`} description={description}>
  <article>
    <div class="project-head">
      <div class="row">
        <h1 style={`view-transition-name: ${projectTransitionName(project)}`}>{name}</h1>
        <span class={`status ${status}`}>{statusLabel(status)}</span>
      </div>
      <p class="desc">{description}</p>
      <div class="tags">{tags.map((tag) => <span>{tag}</span>)}</div>
      <div class="links">
        {demo && <a href={demo} rel="noopener" target="_blank">visit →</a>}
        {repo && <a href={repo} rel="noopener" target="_blank">source →</a>}
      </div>
    </div>

    <div class="prose">
      <Content />
    </div>

    <p class="back"><a href="/projects">← All work</a></p>
  </article>
</BaseLayout>

<style>
  .project-head {
    padding-top: 40px;
    margin-bottom: 40px;
    padding-bottom: 32px;
    border-bottom: 1px solid var(--color-line);
  }

  .row {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    gap: 16px;
  }

  h1 {
    font-family: var(--font-display);
    margin: 0;
    font-weight: 700;
    font-size: clamp(34px, 6vw, 52px);
    letter-spacing: -0.02em;
    line-height: 1.05;
  }

  .status {
    font-family: var(--font-mono);
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--color-ink-faint);
    white-space: nowrap;
  }
  .status.live {
    color: var(--color-good);
  }
  .status.broken {
    color: var(--color-accent);
  }
  .status.wip {
    color: var(--color-ink-dim);
  }

  .desc {
    margin-top: 12px;
    color: var(--color-ink-dim);
    font-size: 16px;
  }

  .tags {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    margin-top: 14px;
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--color-ink-faint);
  }

  .links {
    display: flex;
    gap: 18px;
    margin-top: 18px;
    font-family: var(--font-mono);
    font-size: 11px;
  }
  .links a {
    color: var(--color-accent);
    text-decoration: none;
    border-bottom: 1px solid var(--color-line);
  }
  .links a:hover {
    border-bottom-color: var(--color-accent);
  }

  .prose {
    font-family: var(--font-sans);
    font-size: 19px;
    line-height: 1.7;
    color: var(--color-ink);
  }
  .prose :global(p),
  .prose :global(ul) {
    margin: 0 0 20px;
  }
  .prose :global(ul) {
    padding-left: 1.2em;
  }
  .prose :global(li + li) {
    margin-top: 8px;
  }
  .prose :global(a) {
    color: var(--color-accent);
  }
  .prose :global(code) {
    font-family: var(--font-mono);
    font-size: 0.8em;
    background: var(--color-bg-soft);
    padding: 0.15em 0.4em;
    border-radius: 4px;
  }

  .back {
    margin-top: 56px;
    font-family: var(--font-mono);
    font-size: 11px;
  }
  .back a {
    color: var(--color-ink-dim);
    text-decoration: none;
    border-bottom: 1px solid transparent;
  }
  .back a:hover {
    color: var(--color-accent);
    border-bottom-color: var(--color-accent);
  }
</style>
